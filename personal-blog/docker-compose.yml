version: '3.9'
services:
  backend:
    build: ./backend
    environment:
      DATABASE_URL: mysql+pymysql://pay:9J7pWfK2zEDqR@47.115.225.64:3306/personal_blog?charset=utf8mb4
      SECRET_KEY: super-secret-key
      ADMIN_USERNAME: admin
      ADMIN_PASSWORD: admin123
    ports:
      - "5000:5000"

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    depends_on:
      - backend
    ports:
      - "8080:80"
